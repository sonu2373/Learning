package TestPackage;

import java.util.*;

public class Char_digit {
	public static void main(String args[])
	{
		/*
	System.out.print("Enter an input:");

	//use scanner to get the user input
	Scanner s = new Scanner(System.in);

	// gets the user input
	char[] value = s.nextLine().toCharArray();

	// ask for radix input
	System.out.print("Enter the radix:");

	// get the radix
	int radix = s.nextInt();
	
	// close the scanner object
	s.close();

	// convert user input to digit
	for (char ch : value) {
		int digit = Character.digit(ch, radix);
		// print the result
		System.out.println("character " + ch + " using radix " 
				+ radix + " numeric value is " + digit);}

		
	//System.out.print(Character.MAX_RADIX);
		
	//Binary to decimal conversion
    System.out.println(Integer.parseInt("101010",2));

    //Octal to decimal conversion
    System.out.println(Integer.parseInt("463",8));

    //Hexadecimal to decimal conversion
    System.out.println(Integer.parseInt("4AC",16));

    //String to decimal conversion - throughs NumberFormat Exception, Because it contains D
    System.out.println(Integer.parseInt("6BC",13));
    
    int i=23;
   
		
		
    // convert int to String with given radix
    String g = Integer.toString( i, 36 /* radix 2 to 36  );

    // convert String to int when string is encoded with a given radix
    int d = Integer.parseInt( g.trim(), 36  radix 2 to 36  );
    System.out.print("G="+g+"d="+d);
    
    
		
    int i=Character.forDigit(21, 36);
    
    //System.out.print(Character.getName(i));
    int code=121;
    char c=(char)code;
    
    System.out.print("Value="+c+" "+Character.getNumericValue(code));
  
		System.out.print(Character.isAlphabetic(65));
		System.out.print(Character.isWhitespace(' '));
		
		System.out.println(Integer.decode("0x123"));
		System.out.println(Integer.parseInt("12",16));
		System.out.print(Integer.getInteger("13343"));
    
     */
		System.out.print("str".endsWith("z"));
	    
    
}
}
